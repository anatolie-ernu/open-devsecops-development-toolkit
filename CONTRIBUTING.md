# Contributing

Use only fictional names, domains, addresses and tokens. Never copy data from
private repositories. Run scripts/validate-configs.sh and submit a focused pull
request with documentation and tests.

